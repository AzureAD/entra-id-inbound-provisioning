version: '3.8'

services:
  entra-provisioning-client:
    build: .
    ports:
      - "3001:3001"
    environment:
      - HOST=0.0.0.0
      - NODE_ENV=production
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
